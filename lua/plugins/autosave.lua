return {
  {
    "Pocco81/auto-save.nvim",
    config = function()
      require("auto-save").setup({
        -- I can add other config here later
        -- Or better still I can leave it empty :)
      })
    end,
  },
}
